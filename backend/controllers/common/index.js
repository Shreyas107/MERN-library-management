const { fetchAllBooks } = require("./fetchAllBooks");
const { loginUser } = require("./loginUser");
const { registerUser } = require("./registerUser");

module.exports = {
  fetchAllBooks,
  loginUser,
  registerUser,
};
